-- script that creates a table called first_table in the current database in your MySQL server.
-- A query to create a new table
CREATE TABLE IF NOT EXISTS first_table(
   id INT,
   name VARCHAR(256)
);


